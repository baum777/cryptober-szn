{
  "name": "cryptober-szn",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "audit:sitemap": "node tools/audit-sitemap.js",
    "i18n:scan": "node tools/i18n-scan.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report reports/e2e-html",
    "lint": "eslint js/ utils/ tests/",
    "typecheck": "tsc --noEmit",
    "generate:sitemap": "node scripts/generate-h1-sitemap.js",
    "generate:report": "node scripts/generate-evaluation-report.js",
    "qa:full": "npm run generate:sitemap && npm run generate:report && npm run test:e2e"
  },
  "devDependencies": {
    "cssnano": "^7.1.1",
    "terser": "^5.44.0",
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "@vitest/ui": "^1.0.0",
    "eslint": "^8.55.0",
    "eslint-plugin-import": "^2.29.0",
    "typescript": "^5.3.0",
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.1"
  }
}
